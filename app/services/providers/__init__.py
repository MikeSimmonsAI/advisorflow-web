"""Outbound service providers a customer workspace transacts with.

An energy retailer today, whatever a later vertical needs after that. One
boundary, one normalized status vocabulary, one transaction record — see
`base.py` for the contract every adapter implements and why.
"""
